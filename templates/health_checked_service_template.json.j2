{
    "service": {
        "name": "{{ network_name }}-{{ protocol }}",
        "id": "{{ instance_id }}-{{ network_name }}-{{ protocol }}",
        "port": {{ service_port }},
        "connect": {
            "sidecar_service": {}
        },
        "check": {
            "http": "http://localhost:{{ health_check_port }}",
            "method": "GET",
            "interval": "15s",
            "timeout": "5s"
        }
    }
}